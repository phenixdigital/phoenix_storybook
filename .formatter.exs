# Used by "mix format"
[
  plugins: [Phoenix.LiveView.HTMLFormatter],
  inputs: [
    "*.{ex,exs}",
    "{config,lib,priv}/**/*.{ex,exs,eex}",
    "test/phx_live_storybook/**/*.{ex,exs}",
    "test/*.{ex,exs}"
  ]
]
